{
	"type": "module",
	"scripts": {
		"dev": "set -a && { [ ! -f .env ] || . ./.env; } && set +a && tsx watch apps/server/src/server.ts",
		"server": "set -a && { [ ! -f .env ] || . ./.env; } && set +a && tsx apps/server/src/server.ts",
		"typecheck": "tsc -b tsconfig.json"
	},
	"dependencies": {
		"ink": "^7.0.6",
		"react": "^19.2.7"
	},
	"devDependencies": {
		"@types/node": "^25.9.2",
		"@types/react": "^19.2.17",
		"tsx": "^4.22.4",
		"typescript": "^6.0.3"
	}
}
